{
    "$schema": "../../runner-schema.json",
    "args": "init --git-dir '$TMP'",
    "expected": [
        0,
        [
            "Initialized empty Cov repository in $TMP/.git/.covdata",
            " Using Git repository in $TMP/.git\n"
        ],
        ""
    ],
    "prepare": [
        "mkdirs '$TMP/subdir'",
        "cd '$TMP'",
        "git init",
        "cd '$TMP/subdir'"
    ]
}
