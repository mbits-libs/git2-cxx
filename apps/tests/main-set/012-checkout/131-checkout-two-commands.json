{
    "$schema": "../../../../build/downloads/runner-schema.json",
    "args": "checkout -b --orphan",
    "expected": [
        2,
        "",
        [
            "usage: cov checkout [-h] [--detach|-b|--orphan] <name> [<start-point>]",
            "cov checkout: error: argument -b: not allowed with argument --orphan\n"
        ]
    ],
    "prepare": [
        "unpack $DATA/revparse.tar $TMP",
        "cd $TMP/revparse"
    ]
}
