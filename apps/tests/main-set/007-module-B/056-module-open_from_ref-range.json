{
    "$schema": "../../runner-schema.json",
    "args": "module v1.0.0..HEAD",
    "expected": [
        2,
        "",
        [
            "usage: cov module [-h] [--show-sep [<git-commit>]] | [--set-sep <value> | --add <name> <dir> | --remove <name> <dir> | --remove-all <name>] | [<git-commit>]",
            "cov module: error: <git-commit> argument should describe a single object\n"
        ]
    ],
    "prepare": [
        "unpack $DATA/repo.git.tar $TMP",
        "cd $TMP/repo.git"
    ]
}
