{
    "$schema": "../../runner-schema.json",
    "lang": "pl",
    "args": "config",
    "expected": [
        0,
        [
            "użycie: ",
            " cov config [-h] [<opcje-pliku>] <nazwa> [<wartość>]",
            " cov config [-h] [<opcje-pliku>] --add <nazwa> <wartość>",
            " cov config [-h] [<opcje-pliku>] --get <nazwa>",
            " cov config [-h] [<opcje-pliku>] --get-all <nazwa>",
            " cov config [-h] [<opcje-pliku>] --unset <nazwa>",
            " cov config [-h] [<opcje-pliku>] --unset-all <nazwa>",
            " cov config [-h] [<opcje-pliku>] -l | --list",
            "",
            "opcje pliku:",
            " <brak opcji>            odczytuje ze wszystkich dostępnych plików jednocześnie, zapisuje do .covdata/config",
            " --local                 wybiera .covdata/config",
            " --global                wybiera ~/.config/cov/config",
            " --system                wybiera $(prefix)/etc/covconfig",
            " -f, --file <plik>       wybiera podany plik",
            "",
            "argumenty pozycyjne:",
            " <nazwa> [<wartość>]     gdy podana jest tylko nazwa, odczytuje jej wartość; w przeciwnym razie ustawia nową wartość",
            "",
            "argumenty opcjonalne:",
            " -h, --help              pokazuje ten komunikat pomocy i wychodzi",
            " --add <nazwa> <wartość> dodaje nowy wiersz do opcji bez zmieniania istniejących wartości",
            " --get <nazwa>           drukuje wartość odpowiadającej nazwie klucza",
            " --get-all <nazwa>       drukuje wszystkie wartości pasujące do nazwy klucza",
            " --unset <nazwa>         usuwa wartość pasującą do nazwy klucza, jeśli istnieje dokładnie jedna",
            " --unset-all <nazwa>     usuwa wszystkie wartości pasujące do nazwy klucza",
            " -l, --list              wyświetla listę wszystkich zmiennych w plikach konfiguracyjnych\n"
        ],
        ""
    ],
    "prepare": [
        "mkdirs $TMP/repo.covdata",
        "cd $TMP/repo.covdata"
    ]
}
